# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env

# OS
.DS_Store
Thumbs.db

# Next.js (already in subdir, but good to have)
.next/
out/
node_modules/

# Starters-stoppers (Statbel VAT survival) raw downloads
embuild-analyses/analyses/starters-stoppers/data/TF_VAT_SURVIVALS.zip
embuild-analyses/analyses/starters-stoppers/data/TF_VAT_SURVIVALS.txt

# Analysis raw downloads are generated in CI; keep the repo lean.
embuild-analyses/analyses/**/data/*
!embuild-analyses/analyses/**/data/.remote_metadata.json

# Exception: vergunningen-aanvragen requires manual uploads (no reliable automated download).
!embuild-analyses/analyses/vergunningen-aanvragen/data/**

# CSV files in results/ are for debugging only (not used by frontend, which uses JSON)
embuild-analyses/analyses/**/results/*.csv

# Large JSON reference files in results/ (frontend uses chunked files in public/data/)
embuild-analyses/analyses/gemeentelijke-investeringen/results/bv_municipality_data.json
embuild-analyses/analyses/gemeentelijke-investeringen/results/rek_municipality_data.json

# Local logs directory
logs/

# Temporary analysis files
tmp/
